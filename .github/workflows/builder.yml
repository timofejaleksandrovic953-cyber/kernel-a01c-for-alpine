name: Build A01 Core Kernel

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout kernel source
      uses: actions/checkout@v4
      with:
        repository: timofejaleksandrovic953-cyber/kernel-a01c-for-alpine
        ref: main

    - name: Install build dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          gcc-aarch64-linux-gnu \
          bc bison flex libssl-dev \
          make python3 git \
          perl libxml-simple-perl libxml-libxml-perl \
          libparse-yapp-perl libdbi-perl
    
    - name: Prepare kernel config
      run: |
        cp a01core.config .config
        # Пути к header-файлам драйвера
        scripts/config --file .config --set-str GT1151_FIRMWARE "firmware1"
        scripts/config --file .config --set-str GT1151_CONFIG "config1"
        scripts/config --file .config --enable GTP_CUSTOM_CFG
        scripts/config --file .config --enable GTP_AUTO_UPDATE
        scripts/config --file .config --enable GTP_POWER_CTRL_SLEEP
        # Отключаем Android-специфичный mrdump — не нужен для Alpine
        scripts/config --file .config --disable MTK_AEE_MRDUMP
        scripts/config --file .config --disable MTK_AEE_IPANIC
        scripts/config --file .config --disable MTK_AEE_AED
        # Включаем CONFIG_GTP_CUSTOM_CFG — определяет GTP_INT_TRIGGER и GTP_WAKEUP_LEVEL
        scripts/config --file .config --enable GTP_CUSTOM_CFG
        # Проверяем, что всё записалось
        grep -E "GT1151|GTP_CUSTOM" .config
        make ARCH=arm64 olddefconfig
        # Проверяем ещё раз — olddefconfig не должен откатить
        grep -E "GT1151|GTP_CUSTOM" .config

    - name: Patch MTK Makefiles for missing headers
      run: |
        # mrdump
        MF="drivers/misc/mediatek/aee/mrdump/Makefile"
        grep -q "drivers/misc/mediatek/aee/mrdump" "$MF" || \
          echo 'subdir-ccflags-y += -I$(srctree)/drivers/misc/mediatek/aee/mrdump/' >> "$MF"
        # ppm_v3
        MF="drivers/misc/mediatek/base/power/ppm_v3/Makefile"
        [ -f "$MF" ] && (grep -q "ppm_v3/src/mach/mt6739" "$MF" || \
          echo 'subdir-ccflags-y += -I$(srctree)/drivers/misc/mediatek/base/power/ppm_v3/src/mach/mt6739/' >> "$MF")
        # spm_v4 — пути узнаем из вашего debug-workflow
        MF="drivers/misc/mediatek/base/power/spm_v4/Makefile"
        if [ -f "$MF" ]; then
          HDR=$(find drivers/misc/mediatek/base/power/spm_v4/ -name "mtk_spm_sleep_internal.h" | head -1)
          HDR_DIR=$(dirname "$HDR")
          grep -q "$HDR_DIR" "$MF" || \
            echo "subdir-ccflags-y += -I\$(srctree)/$HDR_DIR/" >> "$MF"
        fi
        MF="drivers/misc/mediatek/cmdq/v3/Makefile"
        HDR_DIR=$(dirname "$(find drivers/misc/mediatek/cmdq/ -name "cmdq_engine.h" | head -1)")
        echo "ccflags-y += -I\$(srctree)/$HDR_DIR/" >> "$MF"
        echo "=== Patched Makefiles ==="
        tail -2 drivers/misc/mediatek/aee/mrdump/Makefile
        tail -2 drivers/mmc/host/mediatek/ComboA/Makefile
        tail -2 drivers/misc/mediatek/base/power/ppm_v3/Makefile 2>/dev/null
        tail -2 drivers/misc/mediatek/base/power/spm_v4/Makefile 2>/dev/null

    - name: Patch cmdq Makefile
      run: |
        # cmdq_engine.h лежит где-то в cmdq/v3/
        HDR=$(find drivers/misc/mediatek/cmdq/ -name "cmdq_engine.h" | head -1)
        echo "cmdq_engine.h found at: $HDR"
        HDR_DIR=$(dirname "$HDR")
        echo "Will add: $HDR_DIR"
        MF="drivers/misc/mediatek/cmdq/v3/Makefile"
        [ -f "$MF" ] || MF="drivers/misc/mediatek/cmdq/Makefile"
        LINE="ccflags-y += -I\$(srctree)/$HDR_DIR/"
        grep -qF "$LINE" "$MF" || echo "$LINE" >> "$MF"
        echo "=== $MF ==="
        cat "$MF"
    
    - name: Build kernel
      run: |
        export ARCH=arm64
        export CROSS_COMPILE=aarch64-linux-gnu-
        export KCFLAGS="-Wno-error=array-bounds \
                        -Wno-error=implicit-function-declaration \
                        -Wno-error=array-compare \
                        -Wno-error=unused-variable \
                        -Wno-error=zero-length-bounds \
                        -Wno-error=stringop-overflow \
                        -Wno-error=stringop-overread \
                        -Wno-error=stringop-truncation \
                        -Wno-error=format-truncation \
                        -Wno-error=maybe-uninitialized \
                        -Wno-error=misleading-indentation \
                        -Wno-error=dangling-pointer \
                        -Wno-error=address \
                        -Wno-error=shift-negative-value \
                        -Wno-error=unused-but-set-variable \
                        -Wno-error=restrict \
                        -Wno-error=undef \
                        -Wno-error=enum-int-mismatch \
                        -Wno-error=incompatible-pointer-types \
                        -I$(pwd)/drivers/mmc/host/mediatek/ComboA \
                        -Wno-error=sizeof-array-div"
        make -j$(nproc) Image.gz-dtb

    - name: Build kernel modules
      run: |
        export ARCH=arm64
        export CROSS_COMPILE=aarch64-linux-gnu-
        make -j$(nproc) modules

    - name: Upload kernel artifact
      uses: actions/upload-artifact@v4
      with:
        name: a01core-kernel
        path: |
          arch/arm64/boot/Image.gz-dtb
          drivers/input/touchscreen/**/*.ko
